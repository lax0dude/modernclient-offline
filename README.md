# modernclient-offline

Offline downloads for Modern Client

May take a while to load. Performance is lower than web version. Only JS is supported, as the Web version of WASM is still broken, and so it isn't portable (yet!)
